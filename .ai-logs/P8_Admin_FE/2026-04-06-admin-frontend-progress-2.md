## 阶段日志（增量）
- **日期**：2026-04-06
- **角色**：P8_Admin_FE
- **主题**：审核后台交互增强

## 本次新增
1. `AdminReview.vue` 增加批量选择（selection 列）。
2. 新增批量操作：
   - 批量封禁
   - 批量解除
3. 新增二次确认弹窗（单条封禁/解除）。
4. 新增乐观更新 + 失败回滚（批量场景）。
5. 新增详情抽屉（展示决策/命中词/AI结果/原文）。
6. `api/admin.js` 对接幂等键流程（先取 key 再提交操作）。

## 价值
- 审核效率提升（批量处理）。
- 误操作风险下降（二次确认 + 幂等）。
- 追溯信息可见性增强（详情抽屉）。
