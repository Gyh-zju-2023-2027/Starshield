## 阶段日志（增量）
- **日期**：2026-04-06
- **角色**：P7_Performance
- **主题**：检索分页与数据量控制

## 本次新增
1. 检索接口新增 `page/limit` 参数，默认值已下沉到后端。
2. `limit` 上限收敛到 1000，降低大查询对 DB/ES 的冲击。
3. ES 优先检索路径已接入，可用于后续压测对比（ES vs MySQL）。

## 压测建议
- 分别在 `es-enabled=true/false` 下测 P95、吞吐和 CPU 占用。
- 重点观察高 page/limit 组合下响应时间曲线。
