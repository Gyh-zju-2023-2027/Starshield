## 阶段日志（增量）
- **日期**：2026-04-06
- **角色**：P7_Performance
- **主题**：审核链路重复请求防护

## 1. 核心提示词 (Prompt)
"请记录幂等键与审计对压测的影响：前后端先取 key 再提交、409 冲突率、审计写入对 P95 的影响，并给出压测关注点。"

## 本次新增
1. 后台操作引入幂等键（5分钟有效期），重复请求返回 `409`。
2. 前端操作链路改为“先取幂等键再提交”，降低并发点击造成的重复写入。
3. 审计日志表落地，便于压测时核验操作次数与结果一致性。

## 压测关注点（新增）
- 幂等键接口 QPS 与成功率。
- `409` 冲突率是否与预期重复请求量一致。
- 审计日志写入对操作接口 P95 的影响。
